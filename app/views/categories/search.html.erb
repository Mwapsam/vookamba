<div class="container mt-5">
    <div class="card p-3">
        <h3>Search Results</h3>
        <% @results.each do |result| %>
            <%= link_to result.name, company_path(result.id), data: {turbo: false}, class: "text-primary" %><br>
        <% end %>
    </div>
</div>